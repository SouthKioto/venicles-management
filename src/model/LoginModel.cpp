#include "../include/model/LoginModel.hpp"
#include "../include/classes/User.hpp"

void LoginModel::setUserData(User *user) { this->user = user; }

bool LoginModel::checkUserExist(User *user, Database *database) {
  int zmienna = 1;
  return true;
}
